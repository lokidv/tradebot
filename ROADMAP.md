# نقشهٔ راه بهبود هستهٔ پیش‌بینی CTP

> ## ⚠️ وضعیت واقعی (به‌روزرسانی ۱۴۰۵/۰۶/۲۳ — ممیزی هسته)
>
> بخش‌های پایینِ این سند **تاریخچه‌اند و چند ادعای نادرست دارند**. وضعیت سنجیده‌شده:
>
> | موضوع | ادعای قدیمی | واقعیت در کد/داده |
> |---|---|---|
> | Champion/Challenger | «انجام شد ✅، held=True تأیید شد» | **غیرفعال بود.** `calib._train_tf` هرگز `champion` را پاس نمی‌داد؛ `held=None` روی هر ۸ مدل در `calib.json`. مدل‌های edge/policy/action اصلاً پارامتر champion ندارند. |
> | مدل‌های معتبر | «هستهٔ نسل ۳ فعال» | در ساختِ ۲۰۲۶-۰۹-۱۱ **هیچ** مدلی روی هیچ تایم‌فریمی از گیت اعتماد رد نشد (setup/dir/edge/policy/action). |
> | لبهٔ ستاپ‌ها | «+۰٫۰۵R هر سیگنال» | نرخ برد پایه: 15m ۳۵٫۵٪ · 1h ۳۶٫۶٪ · 4h ۳۸٫۰٪ · 1d ۳۹٫۷٪ در برابر سربه‌سرِ **۳۵٫۷٪**. بازده خالص پس از هزینه: 15m −۰٫۲۱R، 1h −۰٫۰۷R، 4h +۰٫۰۱R، 1d +۰٫۰۷R (بدون فاندینگ). |
> | «جیب زندهٔ» `edge_book` | مسیر حرفه‌ای ورود | انتخابِ پس از مشاهده روی ۱۲ سطلِ کوچک؛ با لبهٔ صفر احتمال ساخت جیبِ کاذب **۸۳–۹۹٪**. از فاز ۰ دیگر مرجع ورود نیست. |
> | «پیش‌بینی جهت هرگز مجوز معامله نیست» | ✅ | در `positions.json` هفت معاملهٔ ربات با درجهٔ «جهت» ثبت شده بود. |
> | ویژگی‌های کلان | «طلا + شاخص دلار داریم» | `dxy_daily.json` صفر ردیف دارد (ویژگی مرده)؛ فاندینگ برای ۷۶٪ نمادها کمتر از ۸ ردیف است. |
>
> **قفل فعلی:** ورود فقط از ترکیب‌های پیش‌ثبت‌شده در `bot/data/gates.json` مجاز است
> (اکنون خالی ⇒ هیچ معامله‌ای). پول واقعی به `gates.live_allowed` + متغیر محیطی
> `TRADERBOT_LIVE_ACK` نیاز دارد و آداپتور شبکهٔ اصلی عمداً نوشته نشده است.
>
> نقشهٔ راهِ جایگزین در پلن بازسازی (فازهای ۰ تا ۴) است.

---

# 🔬 فاز ۲ — تشخیصِ مبتنی بر شواهدِ زنده (به‌روزرسانی)

## آنچه ۱۷۵ سیگنالِ داوری‌شدهٔ زنده می‌گوید (داورِ نهایی، نه بک‌تست)

| شاهد | عدد | معنی |
|---|---|---|
| وین‌ریتِ زندهٔ کل | ۴۸٫۰٪ (n=۱۷۵) | بالای نقطهٔ سربه‌سرِ ~۳۶٪ در ساختار 1.8R ✓ |
| میانگین R هر سیگنال | **+۰٫۰۵R** | لبه هست ولی **تیغ‌مانند نازک** — کارمزد نصفش را می‌خورد |
| ستاپ‌های ضعیفِ زنده | pb=۴۱٫۷٪، fd=۴۱٫۷٪ | این دو ستاپ در عمل زیانده‌اند (نمونه کم ولی هشداردهنده) |
| ستاپ‌های سالم | dm=۵۰٪، zx=۴۸٫۶٪، sq=۴۸٪ | موتورِ اصلیِ لبه |
| بی‌ثباتیِ مدل | لیفتِ 4h بین بازسازی‌ها: +۱۴٫۷ → −۰٫۶! | **واریانسِ آموزش** — مشکلِ جدیِ حرفه‌ای‌بودن |

## ریشهٔ «سیگنال‌ها خوب نیستند» — پنج عیبِ مشخص

1. **لبهٔ نازک منهای هزینه:** +۰٫۰۵R یعنی هر معامله به‌طور متوسط فقط ~۰٫۱٪ می‌سازد — کارمزدِ ۰٫۱-۰٫۴٪ اکثرش را می‌بلعد. سیگنال باید گزیده‌تر شود، نه بیشتر.
2. **دو ستاپِ زیانده هنوز فعال‌اند** (pb و fd با ۴۱٫۷٪ زنده) و میانگین را پایین می‌کشند.
3. **بی‌ثباتیِ مدل بین بازسازی‌ها:** یک seed، یک snapshot — لیفت 4h از +۱۴٫۷ به −۰٫۶ نوسان کرد. مدلِ جدید بدونِ اثباتِ برتری جایگزینِ قبلی می‌شود.
4. **مدلِ واحد برای همهٔ رژیم‌ها:** وقتی فازِ بازار عوض می‌شود (مثل 4h اخیر)، مدلِ عمومی می‌شکند.
5. **کوریِ نسبت به «وضعیتِ کلِ بازار»:** ویژگی‌ها ۹۰٪ تک‌ارزی‌اند؛ پهنای بازار (چند درصد ارزها بالای میانگین‌اند)، دامیننسِ BTC و ETH/BTC غایب‌اند.

## نقشهٔ ارتقا — به ترتیبِ اثرِ واقعی

> **وضعیت واقعی: P1 نیمه‌کاره است.** ⚠️ ادعای زیر نادرست بود.

### 🥇 P1 — حلقهٔ یادگیریِ زنده
- **تعلیقِ خودکارِ ستاپِ زیانده از رویِ آمارِ زنده:** 🟡 سیم‌کشی شد، ولی حلقه در بن‌بست بود: فقط سیگنال‌های `tradeable` در لاگ سایه ثبت می‌شدند و چون هیچ مدلی معتبر نبود، هیچ ردیفی ثبت نمی‌شد (از ۱۶ جولای صفر رکورد).
- **Champion/Challenger:** ❌ **پیاده نشد.** شاخه‌اش در `_fit_model` وجود دارد ولی `_train_tf` هرگز آن را صدا نمی‌زند؛ هر بازسازی همهٔ مدل‌ها را بی‌قید عوض می‌کند. جایگزین در پلن: بازسازی هفتگی + هیسترزیس دو ساخت متوالی + آزمون منجمد.
- **میانگین‌گیریِ چند-seed** برای MLP: ✅ انجام شد (۵ seed).

### ⚡ فاز ۳ (تیر ۱۴۰۵) — نودهای یادگیریِ موازی ✅ انجام شد
- ۱۶ نودِ یادگیری (پردازه‌های مستقل، بدون GIL): هر فولدِ Walk-Forward و هر seedِ شبکهٔ عصبی یک تسکِ جدا
- دریافتِ موازیِ فاندینگ (×۸) و تاریخچه (×۶) + آموزشِ هم‌زمانِ همهٔ تایم‌فریم‌ها
- نتیجهٔ سنجیده: `_fit_model` از ۱۳.۳s به ۶.۵s با کارِ بیشتر (۵ seed به‌جای ۳ + جست‌وجوی ۳ پیکربندیِ LightGBM)
- استخرِ آموزش ۶۰ → ۱۰۰ ارز — ⚠️ ولی این ۱۰۰ ارزِ پرحجمِ **امروز**اند: سوگیریِ بقا و نگاه به آینده در انتخاب
- ❌ «قهرمان/چالشگر فعال (held=True تأیید شد)» — این ادعا نادرست است؛ در کد هرگز فعال نشد

### 🥈 P2 — ویژگی‌های «وضعیتِ بازار» (رایگان با REST، اثرِ بالا)
- **پهنای بازار (Breadth):** ٪ از ۲۰۰ ارز بالای EMA50 خودشان + ٪ سبزِ ۲۴س — قوی‌ترین سیگنالِ فازِ بازار، از کندل‌های کش‌شده مجانی درمی‌آید.
- **دامیننسِ BTC** (سهمِ حجمِ BTC از کل) و **مومنتومِ ETH/BTC**.
- **ساختارِ زمانیِ نوسان:** نسبت ATR کوتاه/بلند (فشردگی/انبساط).
- **روزِ هفته** (فقط ساعت داریم).

### 🥉 P3 — مدل و برچسبِ بهتر
- **مدلِ per-regime** (رونددار/رنج/پرنوسان جدا) یا حداقل ویژگی‌های تعاملیِ رژیم×سیگنال.
- **بازآموزیِ ۶ساعته** به‌جای ۲۴ساعته (بازارِ کریپتو سریع می‌چرخد) — با champion/challenger امن می‌شود.
- **برچسبِ چندافقی:** به‌جای فقط بریرِ 1.8R، ترکیبی از ۲-۳ افق تا مدل «مسیر» را بفهمد نه فقط یک نقطه.
- آستانهٔ p هر ستاپ جداگانه برای بیشینه‌کردنِ EV زنده تیون شود.

### ⛔ صادقانه خارج از دسترس (نیازمند فیدِ داده‌ای که نداریم)
دفترِ سفارش L2، تاریخچهٔ عمیقِ OI/لیکوییدیشن، اسکیوِ آپشن، دادهٔ تیک. با REST عمومی نمی‌شود؛ اگر روزی WebSocket/دیتابیس اضافه شود، اولویتِ اول همین‌ها هستند.


> سندی صادقانه و اولویت‌بندی‌شده. مرتب بر اساس **بازده به تلاش** برای مشکل واقعی شما (ضررِ حساب دمو) — نه بر اساس جذابیت فنی.

---

## ۰) واقعیتِ سقف — بدون این، بقیه بی‌معناست

پیش‌بینی قطعیِ جهتِ کوتاه‌مدت کریپتو از روی قیمت/حجم، سقفِ پایینی دارد. حتی صندوق‌های کمّیِ کلاس‌جهانی به دقتِ جهتی ~۵۲ تا ۵۵٪ می‌رسند و از سه راه پول درمی‌آورند، **نه** از یک پیش‌گوی جادویی:
1. جمعِ چند لبهٔ کوچک،
2. اجرای دقیق و کنترل هزینه،
3. مدیریت ریسک و حجم.

پس «بهترکردن هسته» دو چیزِ کاملاً متفاوت است:
- **الف) بزرگ‌ترکردن لبهٔ پیش‌بینی** — سخت، با بازده نزولی.
- **ب) تبدیل هر لبه‌ای که هست به بقای سرمایه** — آسان‌تر، بازده بالا. (این همان جایی است که شما ضرر می‌کنید: وین‌ریت ۵۶٪ ولی ضریب سود ۰٫۳۴ — یعنی مشکل شما پیش‌بینی نیست، **حجم و همبستگی** است.)

**نتیجهٔ صریح: بیشترین سودِ فوری از تیر ۱ و تیر ۴ می‌آید، نه از افزودن ویژگی یا مدلِ فانتزی.**

---

## تیر ۱ — پایه‌هایی که همه‌چیز را قابل‌اعتماد می‌کنند (اول این)

| # | کار | چرا | وضعیت |
|---|---|---|---|
| ۱.۱ | **اعتبارسنجی Walk-Forward با embargo** (به‌جای تقسیم ثابت ۷۰/۳۰) | تقسیم تک‌مرحله‌ای، نشتی و خوش‌بینیِ کاذب می‌دهد. پنجرهٔ گسترشی + فاصلهٔ embargo (روش López de Prado). | ✅ **انجام شد** (۵ فولد، embargo=24) |
| ۱.۲ | **ردیاب سایه به‌عنوان داورِ نهایی** + تشخیص drift | بک‌تست دروغ می‌گوید؛ عملکرد زنده نه. | ✅ زیرساخت + پرچمِ drift — باید داده جمع شود |
| ۱.۳ | **هزینهٔ واقعیِ هر ارز** (نقدینگی) | سیگنالِ +۰٫۳٪ روی آلتِ کم‌نقد می‌میرد. | ✅ **انجام شد** (۰٫۱٪ تا ۰٫۴٪ بر اساس حجم) |
| ۱.۴ | **نسخه‌گذاری مدل + بازسازی خودکار** | ناسازگاریِ مدل قدیم. | ✅ انجام شد (نسخهٔ ۵) |
| C1 | **رفعِ نشتیِ جهتِ تایم‌بالاتر** (ممیزی) | آموزش/اجرا ناسازگار بود. | ✅ **رفع شد** |
| C2 | **رفعِ نشتیِ نرمال‌سازیِ طلا/دلار** (ممیزی) | انحراف‌معیارِ کلِ تاریخچه = نشتی آینده. | ✅ **رفع شد (علّی)** |

---

## تیر ۲ — ویژگی‌های بهتر/بیشتر (خواستهٔ «داده بیشتر»، ولی دقیق)

| # | کار | قدرت پیش‌بینی | وضعیت |
|---|---|---|---|
| ۲.۱ | **دفترِ سفارش/میکروساختار** — عدم‌توازنِ بید/اَسک، عمق | 🔥🔥🔥 بزرگ‌ترین منبعِ دست‌نخورده | ⛔ نیازمند فیدِ زندهٔ L2 (این ابزار REST است) |
| ۲.۲ | **Open Interest + لیکوییدیشن‌ها** | 🔥🔥🔥 | ⛔ بایننس فقط ~۳۰ روز تاریخچهٔ OI می‌دهد — برای آموزش کافی نیست |
| ۲.۳ | **دامیننس BTC، ETH/BTC، جریان استیبل‌کوین** | 🔥🔥 | ⬜ قابل افزودن |
| ۲.۴ | **بیسِ پرپچوال + اسکیو آپشن‌ها** | 🔥🔥 | ⛔ نیازمند دادهٔ آپشن (Deribit) |
| ۲.۵ | **ویژگی‌های تعاملی + چندافقی** | 🔥 | 🟡 تا حدی — LightGBM تعاملات را خودکار می‌گیرد |

> ✅ همین حالا داریم: فاندینگ، قدرت نسبی، **طلا (PAXG، حالا علّی)**, **شاخص دلار (حالا علّی)**، تایم‌بالاتر (حالا بدون نشتی)، رژیم.

> ✅ همین حالا داریم: فاندینگ‌ریت، قدرت نسبیِ مقطعی، طلا (PAXG)، شاخص دلار، هم‌راستایی تایم‌بالاتر، رژیم.

---

## تیر ۳ — مدل‌های بهتر (بازده نزولی، بعد از تیر ۱ و ۲)

| # | کار | اثر | وضعیت |
|---|---|---|---|
| ۳.۱ | **LightGBM + انتخابِ خودکارِ برنده** | 🔥🔥🔥 | ✅ **انجام شد** — نصب شد و هر تایم‌فریم بین لجستیک/LightGBM برندهٔ Walk-Forward را می‌گیرد. (نکتهٔ صادقانه: روی این داده، لجستیک اغلب می‌بَرد چون LightGBM روی نویز overfit می‌کند) |
| ۳.۲ | **مدلِ جدا برای هر رژیم** | 🔥🔥 | 🟡 LightGBM تعاملاتِ رژیم را خودکار می‌گیرد؛ مدلِ جدا در صف |
| ۳.۳ | **کالیبراسیونِ احتمال (Platt)** | 🔥 | ✅ **انجام شد** — خروجی همهٔ مدل‌ها پلَت-کالیبره است |
| ۳.۴ | **انسمبلِ چند-مدلی + شبکهٔ عصبی** | 🔥 | ✅ **انجام شد** — مغزِ Stacking: لجستیک + LightGBM + شبکهٔ عصبیِ MLP + متا-مدلِ ترکیب‌کننده؛ برندهٔ Walk-Forward هر تایم‌فریم انتخاب می‌شود |

---

## تیر ۴ — تبدیل لبه به بقا (بیشترین بازده روی ضررِ واقعیِ شما) ⭐

| # | کار | چرا مستقیماً ضرر شما را کم می‌کند | وضعیت |
|---|---|---|---|
| ۴.۱ | **حجم بر اساس ریسک** | ضریب سود ۰٫۳۴ محصولِ حجم است. | ✅ حالتِ «حجم از روی ریسک» در تنظیمات |
| ۴.۲ | **سقفِ ریسکِ پرتفوی + همبستگی** | ۱۴ لانگِ همبسته = یک شرطِ بزرگ. | ✅ **انجام شد** — سقفِ گرمای ۶٪ + حداکثر ۵ پوزیشنِ هم‌جهت، با هشدارِ آگاهانه |
| ۴.۳ | **حدضرر/هدفِ داینامیک از نوسان** | حدِ ثابت زود می‌خورد. | ✅ از ATR ساخته می‌شود (داریم) |
| ۴.۴ | **متا-لیبلینگ برای فیلترِ معامله** | مدلی که می‌گوید «آیا *این* سیگنال کار می‌کند». | ✅ داریم (مدلِ ستاپ + جهت‌یاب) |

---

## تیر ۵ — سازگاری با تغییرِ بازار

| # | کار | اثر |
|---|---|---|
| ۵.۱ | **بازآموزیِ نوردهٔ هفتگی + تشخیصِ drift** | وقتی عملکردِ زندهٔ یک مدل افت کرد، خودکار خاموش شود |
| ۵.۲ | **تشخیصِ رژیم پیشرفته (HMM / changepoint)** | سوییچِ استراتژی با تغییرِ فاز بازار |

---

## توصیهٔ قدمِ بعدی (اگر یکی را انتخاب کنید)

1. **بیشترین بازدهِ فوری برای ضررِ شما → تیر ۴.۱ + ۴.۲** (حجم بر اساس ریسک + سقفِ همبستگی). این تنها چیزی است که ضریب سودِ ۰٫۳۴ را واقعاً بالا می‌برد.
2. **بیشترین ارتقای پیش‌بینی → تیر ۳.۱** (LightGBM) — ولی فقط بعد از تیر ۱.۱ (Walk-Forward)، وگرنه روی دادهٔ نشتی‌دار بهینه می‌کنیم.
3. **بزرگ‌ترین منبعِ سیگنالِ جدید → تیر ۲.۲** (Open Interest + لیکوییدیشن).

**پیشنهاد شخصی من:** اول ۲ تا ۴ هفته بگذارید ردیاب سایه (📡) داده جمع کند، بعد بر اساس عملکردِ *زنده* تصمیم بگیریم کدام تیر ارزشِ کار دارد — چون تنها معیارِ صادق همان است. تا آن موقع، **تیر ۴** (حجم/همبستگی) را بسازیم که بدونِ نیاز به پیش‌بینیِ بهتر، از حسابتان محافظت می‌کند.
